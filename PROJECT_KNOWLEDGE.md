# ミニトマト部会 出荷・単価情報 見える化システム

プロジェクトの前提・決定事項・進め方をまとめたナレッジファイル。
新しいセッションを始める際は、まずこのファイルを読み込ませてから話を進める想定。

---

## 0. プロジェクト概要

- **目的**：部会員(約40名)がスマホで、いつでも市場別・サイズ別の単価と出荷量を確認できるようにする
- **利用者**：ミニトマト部会員 約40名。ITリテラシーは高くない前提
- **閲覧デバイス**：スマートフォンがメイン(管理者のデータ入力はPC/タブレット推奨)
- **ステータス**：**部会に提案し採用済み。実装フェーズに移行**
- **公開状況**：モック画面をGitHub Pagesで公開済み → https://yoppy0202.github.io/tomato-dashboard-mock/ (ダミーデータ、本番仕様とは異なる旨のバナーあり)
- **成果物**：
  - モック画面(HTML、GitHub Pages公開)
    - ダッシュボード：`index.html` → https://yoppy0202.github.io/tomato-dashboard-mock/
    - 管理画面・マトリクス入力：`admin.html` → https://yoppy0202.github.io/tomato-dashboard-mock/admin.html
    - データ一覧ページ：`list.html` → https://yoppy0202.github.io/tomato-dashboard-mock/list.html
    - お知らせ投稿：`announce.html` → https://yoppy0202.github.io/tomato-dashboard-mock/announce.html
    - 市場の管理：`markets.html` → https://yoppy0202.github.io/tomato-dashboard-mock/markets.html
  - 提案メモ(Word、4ページ版・部会内共有用)

---

## 1. データの分類軸

「選果方法 × 荷姿 × サイズ × 市場」の掛け合わせが最小単位。

| 分類軸 | 内容 |
|---|---|
| 選果方法 | 個人選果 / 共同選果 |
| 荷姿 | 200gパック / 3kgバラ |
| サイズ | 2L・L・M・S・2S |
| 市場 | 東京・神奈川・徳島・北九州・福岡・長崎・諫早(7市場) |

- 契約により個人・共同それぞれで荷姿が発生する可能性があり、拡張しやすい設計にしておく
- 分類軸はマスタテーブルで管理し、市場・荷姿等が増えても画面改修なしで反映できるようにする
- 出荷は基本毎日発生する

### 市場マスタの管理(確定)※モック作成済み(`markets.html`)
管理者が画面から市場を追加・除外・並べ替えできる。「市場の管理」画面(`/admin/markets`)を用意する。

- **削除は物理削除せず「無効化(論理削除)」**:市場マスタに有効フラグ(`active`)を持たせる
  - 無効化した市場は、入力画面の列・ダッシュボード・絞り込みの選択肢から消える
  - **過去の出荷実績・単価・修正履歴はすべて保持**され、データ一覧で過去期間を選べば従来どおり表示・CSV出力できる
  - 取引再開時は「一覧に戻す」で有効化するだけ(データはそのまま接続される)
  - 物理削除を許すと過去実績・監査ログが参照先を失って壊れるため、UI上も「削除」ではなく「一覧から外す」と表記する
- **表示順は手動並び替え**:市場マスタに並び順(`sort_order`)を持たせ、▲▼ボタンで入れ替え。並び順は**ダッシュボード・入力画面・データ一覧のすべてに共通反映**する(見慣れた順序を管理者が維持できるようにするため)
- 追加時は重複チェックを行い、無効化済みの同名市場がある場合は新規作成ではなく**「一覧に戻す」を案内**(データの二重化を防ぐ)
- 市場マスタの変更(追加・無効化・有効化・並べ替え)は**すべて監査ログに記録**し、同画面に変更履歴として表示する
- 画面には「過去のデータは消えない」旨の説明を常時表示し、管理者が安心して操作できるようにする

### データ整合性の原則(確定)
- 日次実績・監査ログは**市場ID(外部キー)で保持**し、市場名の文字列では持たない(改称時も過去データが追従する)
- **監査ログ・出荷実績のテーブルは市場ごとに分割しない**。1テーブルに市場IDを持たせ、画面側で市場フィルタをかける方式とする
  - 市場ごとにテーブルを分けると、市場追加のたびにテーブルが増え、集計・前年比較・CSV出力が市場をまたげなくなるため
  - 「市場ごとに追える」要件は、**ログ画面・データ一覧の市場フィルタ**で満たす
- 監査ログには市場IDを持たせ、**市場単位での絞り込み表示**を可能にする

### 数量の単位とkg換算(確定)
- 数量は**実際の個数**で扱う：200gパック=**パック数**、3kgバラ=**ケース数**
- kg換算ルール：**200gパックは5パックで1kg(パック数÷5)、3kgバラは1ケース=3kg(ケース数×3)**
- 入力は個数、画面・CSVには**個数とkg換算を併記**する(換算はシステムが自動計算。手計算させない)
- 換算係数(1パック=0.2kg、1ケース=3kg)は荷姿マスタに持たせ、荷姿追加時も画面改修なしで対応できるようにする

### 単価の単位(確定)
- 販売通知の単価は**1パック/1ケースあたり**の価格。入力もそのまま個口単価で行う(kg単価は入力させない)
- 円/kg換算ルール：**200gパックは単価×5、3kgバラは単価÷3**(換算方向が数量と逆になる点に注意)
- 一覧画面では**個口単価をメイン表示**とし、円/kg換算をサブ表示で併記。CSVは両方を出力

---

## 2. 画面構成(モックで確定した設計)

- 最上位タブ：「個人選果」「共同選果」の2つ
- 荷姿はプルダウン選択(個人選果は200gパックが既定、共同選果は3kgバラが既定)
- 全7市場×5サイズの単価を1画面に集約。数量はサブ情報として併記
- **単価は1パック/1ケースあたりで表示**(kg表記なし。荷姿の選択に連動して「1パックあたり/1ケースあたり」のラベルを切替)
- **青数字=前年同日より高値**のサイズ(セル単位で前年比較して着色。赤との対比で識別しやすく、色覚特性にも配慮)
- 市場ごとの**前年比バッジは、アップ=青(▲)・ダウン=赤(▼)・変化なし=グレー(±)**で色分け
- 見出し下に色の見方を1行で説明(「青数字=前年同日より高値のサイズ／市場名の横は市場全体の前年比」)
- 日付は横スワイプの日付ピルで選択。土曜=青、日曜・祝日=赤で色分け
- スクロールすると以下が続く：
  1. **曜日別の出荷量(先週実績)**：最多の曜日を100として棒の高さを算出。目的は**週を通した出荷の平準化**
     - 出荷が少ない曜日(週平均の7割未満)・曜日別平均単価の差(8%超)を**データから自動判定**し、注意書き・提案メッセージを自動生成(バー下の個別ラベルは付けず、メッセージ枠に集約してレイアウトを揃える)
  2. 前年同日比(数量・単価)※**平均単価も1パック/1ケースあたり**で表示
  3. 月別の数量ペース・単価比較(前年対比)
  4. お知らせ(見出しは「お知らせ」。各件にカテゴリバッジ〈全体/個人選果/共同選果〉を表示)

### 画面共通のルール(確定)
- 各画面からの戻りリンクは**「← TOPへ戻る」で統一**(ページごとに文言を変えない)

#### サイト内ナビゲーション(確定)
ダッシュボードを起点とした放射状構造(各ページは「TOPへ戻る」しか持たない)を改め、**共通ナビゲーション帯**で関連ページを相互に行き来できるようにする。管理者が入力→お知らせ投稿→市場管理と作業をまたぐたびにTOPを経由する手間を無くすため。

- **管理者用ナビ帯**(管理者ログイン時のみ表示):データ入力(`admin.html`)/ データ一覧(`list.html`)/ お知らせ投稿(`announce.html`)/ 市場の管理(`markets.html`)/ 議事録(`minutes.html`)の5ページを相互リンク
- **一般ユーザー用ナビ帯**(全員に表示):ダッシュボード(`index.html`)/ データ一覧(`list.html`)/ 議事録(`minutes.html`)の3ページを相互リンク
  - 個別お知らせはダッシュボードの「お知らせ」欄から遷移する形を維持(ナビ項目には入れない)
- ナビ帯は各ページ上部に常時表示。**現在開いているページはハイライトして非活性**(自分自身へのリンクは無効化)
- 表示形態:PC/タブレットは横並び、スマホ幅では横スクロールのピル形式に切替
- 管理者用ナビ帯は**一般ユーザーには帯ごと非表示**(存在を見せない。議事録の役員協議会タブと同じ考え方)
- `admin.html`の既存「管理モード」帯を拡張してナビ帯を兼ねさせる(帯を二重に出さない)

#### 未保存離脱ガード(確定)
入力途中で他ページへ移動して入力内容を失う事故を防ぐため、**全入力画面に共通の離脱ガード**を入れる。

- 対象画面:`admin.html`(マトリクス入力)、`announce.html`(お知らせ下書き)、`markets.html`(並べ替え・追加)、`minutes.html`(議事録の投稿・編集)
- 「未保存の変更があるか」をフラグで管理(入力変更時にON、保存完了時にOFF)
- フラグONの状態で以下が発生したら警告:
  1. 共通ナビ帯のリンククリック → **サイト内確認ダイアログ**で遷移を止める
  2. 「← TOPへ戻る」クリック → 同上
  3. ブラウザの戻る/更新/タブを閉じる → **`beforeunload`によるブラウザ標準の確認**(文言はブラウザ依存で変更不可のため、サイト内ダイアログとは別扱いとする)
- サイト内ダイアログの選択肢は3つ:
  - 「保存してから離れる」…その画面の保存処理を呼び出し、完了後に遷移(下書き保存で足りる画面は下書き保存。`admin.html`のように確定フローがある画面は確定フローに合流させる)
  - 「このまま離れる」…変更を破棄して遷移(赤字表記)
  - 「キャンセル(このページに留まる)」

### 前年比較の単価表示(追加決定)
- 単純平均に加えて、**数量加重平均単価**(販売金額合計 ÷ 出荷数量合計)を併記する
- たくさん出たサイズの単価が強く反映されるため、体感の手取りに近い数字になる
- 画面上のラベルは「平均単価(数量加重)」のような誤解のない表記とする

---

## 3. ログイン方式と権限

### ログイン
- **LINEログイン**を採用(SMS認証は不採用。コストと導入ハードルの両面でLINEが優位という結論)
- 流れ:①「LINEでログイン」を選択 → ②LINEアプリで同意 → ③初回のみ部会員名簿と紐付け → ④以降は自動ログイン

#### 名簿とLINEアカウントの紐付け(確定)
LINEログインで取得できるのはLINE側の内部ID(`userId`)と表示名のみで、
「その人が部会員の誰なのか」はシステム側では判別できない。
表示名はニックネームのことが多く、氏名との突合にも使えないため、初回に明示的な紐付けを行う。

**方式:管理者が先に名簿を登録 → 初回ログイン時に本人が名簿から自分を選択**

1. 管理者が名簿管理画面(`members.html`)で部会員の氏名を事前登録(部会の名簿から転記)
2. 部会員が初回ログイン → 「あなたのお名前を選んでください」と名簿一覧を表示
3. 本人が自分の氏名を選択 → LINEの`userId`と名簿レコードを紐付け
4. 管理者は名簿管理画面で「連携済み / 未連携」を随時確認できる

- 本人に氏名を入力させる方式(→ §11 不採用案)ではなく**選択方式**とする理由:
  表記ゆれが発生せず、ITリテラシーが高くない利用者でも確実に完了できるため
- **氏名は名簿側の値を正**とし、LINEの表示名は紐付け時の参照にのみ使う。
  議事録の投稿者名・既読/未読の氏名一覧など、画面表示はすべて名簿の氏名を使用する
- 機種変更等でLINEアカウントが変わった場合に備え、管理者が**連携解除**して再紐付けできるようにする

### 権限
- **閲覧 / 管理者** の2段階のみ(細かい権限分けはしない)
- 閲覧：ダッシュボード・データ一覧の確認、CSVダウンロード
- 管理者：数量・単価の入力・修正、お知らせ投稿、権限変更
- **管理者は必ず2名以上を確保する**(1名だと本人がログインできなくなった時に誰も管理できなくなるため)
- 同じ画面でも管理者にのみ編集ボタンを表示する作りとし、閲覧用・管理用に画面を二重に作らない

#### 部会員名簿の管理(確定)※モック作成済み(`members.html`)
管理者が部会員の氏名・権限・LINE連携状況を画面から管理できるようにする。
市場マスタ(`markets.html`)と同じ位置づけの管理画面。

- **一覧の表示項目**:氏名 / LINE連携状況(済・未) / 権限(閲覧・管理者) / 最終ログイン日時
- **上部に集計を表示**:「LINE連携済み 34/40人」「管理者 3人」
  (導入時に未連携者が何名残っているかを管理者が把握できるようにするため)
- **絞り込み**:すべて / 未連携 / 管理者。氏名でのキーワード検索も可
- **編集できる項目**:
  - 氏名の修正
  - 権限の切替(閲覧 ⇄ 管理者)
  - 一覧から外す(**論理削除**。市場マスタと同じ方式で過去の記録は保持)
  - LINE連携の解除(機種変更・アカウント再作成時の再紐付け用)
- **管理者が1名になる操作はブロックする**(確認ダイアログではなく操作自体を不可とする)。
  §3「管理者は必ず2名以上」を画面側で担保するため
- 名簿の変更(追加・氏名修正・権限変更・無効化・連携解除)は**すべて監査ログに記録**する
- 画面には「一覧から外しても過去の記録は消えない」旨を常時表示する
- 管理者用ナビ帯に6つ目の項目として追加する(→ §2 サイト内ナビゲーション)

#### モックと本番の差異(実装時の必須事項)
モックでは利便性のため以下の割り切りをしている。**本番実装ではいずれもサーバー側で行うこと**。

- **役割(管理者/一般)の保持**:モックは`localStorage`。利用者が書き換え可能なため、
  本番は**サーバー側のセッションで判定**する
- **管理者専用ページへの直接アクセス**:モックはURLを直接叩けば一般ユーザーでも開ける
  (例:`minutes-edit.html`)。本番は**サーバー側で権限チェックしてリダイレクト**する
- **役員協議会の非表示**:モックは画面側のフィルタ。本番は**クエリ段階で除外**する
  (一覧・検索・詳細の直リンクすべてが対象。詳細は議事録IDを直接指定されても弾くこと)
- **下書きの保存先**:モックは`localStorage`(`announce.html`/`minutes-edit.html`)。
  本番は**サーバー側の下書き保存**とする(→ §4 途中保存)

---

## 4. データの保存・入力・修正(確定)

### 保存先
- **正本**：VPS上の**SQLiteデータベース**に一本化。特定担当者のローカルPCには正本を置かない
  - 40名規模・1日数十行の書き込みならSQLiteで十分。DBがファイル1個なのでバックアップ・引継ぎが軽い
- 日次実績テーブル(日付・選果方法・荷姿・市場・サイズ・単価・数量)を軸に、修正しても履歴が残る構造とする

### 通常入力(管理画面のマトリクス一括入力フォーム)※モック作成済み(`admin.html`)
- 元データは**紙・FAXの販売通知**として届くため、転記入力を主軸とする
- 管理者専用ページ(`/admin`)に、閲覧画面と同じ**7市場×5サイズの表**を表示し、FAXと見比べながら一括転記 → 1回保存
- 数量は**個数で入力**(200gパック=パック数、3kgバラ=ケース数)。単価は**1パック/1ケースあたり**で入力(ラベルにkg表記は付けない)。単位ラベルは荷姿の選択に連動して切替
- 数量セルの直下に**kg換算を自動表示**(パック÷5、ケース×3)。転記の検算に使える
- Tab/Enterキーでセル移動できるようにし、テンキー転記のリズムを崩さない
- 出荷がなかった市場は空欄のままでOK(全セル必須にしない)
- 「前日の値をコピー」ボタンで下書きを作り、差分だけ直す運用も可能に
- 途中保存(下書き)を許可し、確定するまで閲覧画面には反映しない
- **確定時は確認ダイアログを挟む**(対象日・選果方法・荷姿・入力済みセル数を提示 →「この内容で確定」で反映。誤確定防止のワンクッション)
- 保存前の**入力チェック**:前日比で単価が大きく乖離しているセル、数量のみで単価が空のセルを黄色ハイライトして確認を促す(桁間違い・転記ミス対策)
- 入力デバイスはPC/タブレット推奨。スマホでは市場ごとのアコーディオン表示(サイズ／単価／数量／kg換算の4列)に切り替え、軽微な修正に対応
- 画面上部に**「管理モード」の帯**を常時表示し、閲覧画面との取り違えを防ぐ(操作が記録される旨も明記)

### 修正フロー
- 入力フォームで**過去日付を開くとその日のデータが表示され、セルを直して保存し直せる**(管理者の体感は「直接編集」)
- 裏側では修正前後の値・実行者・日時を**修正履歴テーブルに自動記録**(監査ログと統合)
- ダッシュボードには修正後の値を即反映。該当セルに「修正済み」マークを表示し、タップで修正日時を確認可能
- DBを生で触る運用(phpMyAdmin等)は不採用。履歴が残らず監査ログを素通りするため。緊急時の最終手段としてSSH経由の直接操作は残すが、通常運用は必ず画面経由に限定

### バックアップ
- **暗号化バックアップ**：`age`でDBダンプ(SQLiteファイル)を暗号化してから複製
- **保管先**：GitHub(private)＋ローカルなど、最低2箇所に自動保存(1箇所に依存させない)
- 暗号化鍵は最低2箇所(自分のPC＋パスワードマネージャー等)に保管
- 引継ぎ手順(アクセス方法・鍵の保管場所)は別途文書化し、管理者交代時にも困らないようにする

---

## 5. データ一覧ページ(閲覧・修正モード共通/確定)

一般部会員も管理者と同じデータにアクセスできる閲覧専用の一覧ページを用意する(管理者にのみ編集UIを表示)。※モック作成済み(`list.html`)

### 期間選択
- 開始日・終了日の**プルダウン方式**(単日も範囲も選択可能)
- 日付表記に曜日を含める(例:「8/8(土)」)
- 配色はダッシュボードの日付ピルと同一ルール:**土曜=青、日曜・祝日=赤**

### 一覧の表示列
- 日付・選果・市場・荷姿・サイズ・**単価(1パック/1ケースあたり:メイン表示)・単価(円/kg:換算サブ表示)**・数量(個数:ﾊﾟｯｸ/ｹｰｽ)・kg換算
- 修正されたデータには「修正済」マークを表示。タップで修正日時を確認可能
- 画面表示は50件ずつページ分割

### 絞り込みフィルタ(チェックボックス方式)
- 選果方法:個人選果 / 共同選果
- 出荷規格(荷姿):200gパック / 3kgバラ
- 市場:7市場(全選択/全解除ボタン付き)
- サイズ:2L・L・M・S・2S
- 選択肢はマスタテーブルから動的生成(市場・荷姿の追加時に画面改修不要)

### CSV出力(必須)
- フィルタ・期間で絞り込んだ結果をそのままCSVダウンロード
- 列構成:日付・選果方法・荷姿・市場・サイズ・**単価(1パック/1ケースあたり)・単価(円/kg)**・数量・数量単位・kg換算
- 文字コードは**UTF-8(BOM付き)**を既定(Excelでの文字化け防止)
- 長期間選択時、画面表示はページ分割しつつ、CSVは絞り込み条件の**全件**を出力

---

## 6. お知らせ機能

### 未読・既読管理
- ログイン日時ではなく、**お知らせごとの既読記録**(`announcement_reads`テーブル)で判定
- ログイン時に未読のお知らせをポップアップ表示、閉じた/確認した時点で既読登録
- `announcement_reads`は「誰が・どのお知らせを・いつ読んだか」を1件ずつ保持する(集計数だけでなく個人単位の状況を出せる構造)

#### 未読者の把握(確定)
管理者が「誰がまだ読んでいないか」を画面から確認できるようにする。重要連絡が届いていない部会員へのフォローアップを、管理者が自分で判断できるようにするため。

- **管理者ビュー**:お知らせ詳細に既読/未読の内訳を表示
  - 進捗バー＋「28/40人 既読」の集計表示
  - 「未読」タブ:未読者の**氏名一覧**
  - 「既読」タブ:既読者の**氏名＋既読日時**一覧
- **一般ユーザー**:従来どおり**集計数(x/40人)のみ**表示。個人単位の内訳は見せない(誰が読んでいないかを部会員同士で見せ合う必要はないため、内訳は管理者限定)
- 議事録にも同じ既読記録の仕組みを流用できる構造にしておく(将来の展開余地を残す)
- **未読者へのLINE個別リマインド送信はフェーズ1では実装しない**(→ §11 保留案件)

### 配信フロー(セキュリティ・不備対応を統合)
1. お知らせを投稿 → サイトには即反映
2. LINEグループへの配信は**10分間保留**
3. 保留中、投稿者(または他の管理者)は「訂正」(基本はこちら/誤字・数値ミスの修正)または「削除」(安全弁/詐欺リンク混入など重大な問題向け、想定頻度は低い)ができる
4. 10分経過後、その時点の内容でLINEグループへ自動配信。以降は編集不可(直したい場合は新規投稿として出し直す)
5. 投稿があった時点で、個人ではなく**管理者用LINEグループチャット**へ自動通知(身に覚えのない投稿への気づきを早める)

### 投稿画面のUI(確定)※モック作成済み(`announce.html`)
- **カテゴリ(対象)をプルダウンで選択**:全体 / 個人選果 / 共同選果(既定は全体)。一覧では色分けバッジ、LINE配信文では冒頭に【個人選果】のように付与
  - カテゴリは表示上の目印であり、閲覧制限ではない(全員がすべてのお知らせを閲覧できる)
  - DBは「全体」＋選果方法マスタの値で構成し、カテゴリ追加時も画面改修なしで対応
- 画面上部に配信フローの3ステップ(①投稿=即掲載 ②10分保留=訂正・削除可 ③LINE自動配信=以降編集不可)を常時表示
- **文字数カウンター**を表示(推奨上限500字。超過時は赤字警告のみで投稿はブロックしない)
- **LINE配信プレビュー**を入力中に常時表示し、実際に送信される文面を確認できる
- **投稿前に確認ダイアログ**を挟む(本文プレビュー+LINE送信文面+URL安全性チェック結果)
- 危険URL検知時は投稿を**ブロックするダイアログ**を表示(監査ログ記録・管理者グループ通知の旨も明記)
- 配信待ちのお知らせには「サイト掲載中」「LINE配信待ち」バッジと**配信までの残り時間カウントダウン**を表示
- **訂正して再投稿すると保留時間はリセット**(あらためて10分。訂正後の内容にも見直しの猶予を確保するため)
- 削除はサイトからも即時取り下げ、LINE配信を中止(確認ダイアログ付き。軽微な修正は訂正を促す文言)
- 配信済みのお知らせには**既読数(x/40人)**を表示し、編集不可・新規投稿での出し直しを案内
- カテゴリに**「議事録」**を追加(議事録の自動連携で生成されるお知らせ用。手動投稿では選択させない)
- 議事録由来のお知らせは、投稿画面からの編集・削除の対象外(→ §8 お知らせへの自動連携)

### 長文の扱い(確定)
- **LINE配信**:本文200字以下はそのまま全文送信。超える場合は**冒頭150字前後を文の区切り(。、改行)で丸めて送信し、続きは該当お知らせへの直リンク**を付ける
  - 要約はAI生成ではなく**冒頭切り出し方式**(重要連絡で要点を取り違えるリスク回避＋コストゼロ)。書き出しに要点を書く運用を投稿画面で案内
  - 投稿画面のLINEプレビューで管理者が送信文面を事前確認できるため、違和感があれば本文の書き出しを直せる
- **サイト表示**:本文300字を超えたら3行程度で折りたたみ、「続きを読む」で全文表示
  - ただし**未読ポップアップは折りたたまず全文表示**(開く手間を二重にしないため)
- 閾値(LINE 200字/冒頭150字、サイト300字、推奨500字)はすべて設定値とし、運用しながら調整可能にする

### LINEリンクからの遷移(確定)
- リンク先は個別お知らせのURL(例:`/announce/12`)。未ログインの場合はLINEログイン画面を挟み、**認証後は元のお知らせへリダイレクト**
- **リンク経由で開いたお知らせは未読ポップアップの対象から外す**(同じ内容が二重表示されるのを防ぐ)。他に未読があればそちらのみポップアップ表示
- ログイン保持期間は**30日**を既定とする(閲覧専用の情報であり、月数回の利用者がログイン画面をほぼ見ずに済む長さ)

### 画像の扱い(方針決定)
- **フェーズ1はテキスト＋URLのみ。画像添付は後付け候補**とする
- 理由:VPSへの画像保存(リサイズ・バックアップ容量増)が必要になること、Safe Browsingは画像に効かず不適切画像チェックが人力頼りになること
- 当面、画像を送りたい場面はLINEグループへ直接投稿で代替できる。実需が出てから実装判断
- LINE側は画像メッセージ(HTTPS公開URL・JPEG/PNG・最大10MB)で対応可能。テキストと1回のpushにまとめられるため無料枠200通/月の消費は増えない

### LINEグループ通知の仕組み
- LINE公式アカウントを通知先のグループチャットに招待(この初回招待だけは人力操作が必要)
- 招待時に取得できる`groupId`を通知先として設定すれば、以降はAPI経由でグループへpush可能

---

## 7. セキュリティ・不正対策

- **監査ログ**:ログイン試行(成功/失敗)・データ編集(修正履歴を含む)・お知らせ投稿・**市場マスタの変更(追加/無効化/並べ替え)**を管理者画面に記録
- **監査ログの絞り込み**:ログには市場IDを持たせ、**市場単位で絞り込んで確認できる**ようにする(テーブルは市場ごとに分割しない。→ §1データ整合性の原則)
- **日次レポート**:異常の有無を日次で自動集計・報告
- **異常検知の基準例**:同一アカウントでの短時間連続ログイン失敗、深夜帯アクセス、普段と異なるIP、通常操作にない大量データ変更 → 検知時はログイン時にポップアップ警告
- **お知らせリンクの安全性チェック**:投稿時にGoogle Safe Browsing API等でURLを照合し、既知のフィッシング/詐欺サイトなら投稿をブロック(ドメイン固定は不採用。外部の有益情報共有を妨げないため)
- 上記「10分の配信猶予」「管理者グループへの投稿通知」もセキュリティ対策を兼ねる

---

## 8. 議事録機能

部会の議事録をサイト上で共有・検索できるようにする。

### 画面構成 ※モック作成済み
- 一覧・検索:`minutes.html`
- 詳細:`minutes-detail.html`
- 投稿・編集(管理者のみ):`minutes-edit.html`

### カテゴリと閲覧権限(確定)
- カテゴリは**「役員協議会」「全体協議会」**の2種。カテゴリはマスタ管理とし、追加時も画面改修なしで対応
- **管理者**:両カテゴリの閲覧・投稿・編集・削除(論理削除)
- **一般ユーザー**:**全体協議会のみ閲覧**(投稿・編集・コメント等は不可)
- 一般ユーザーには**役員協議会をタブごと非表示**にする(件数表示や「権限がありません」表示も出さず、存在自体を見せない)
- お知らせのカテゴリ(全体/個人選果/共同選果)が「表示上の目印」なのに対し、議事録のカテゴリは**実際のアクセス制御**である点が異なる。実装時に混同しないこと

### 本文と添付ファイル(確定)
- **本文はテキスト入力**(管理画面から直接入力・編集)。検索対象はこの本文とタイトル
- **添付ファイル**を任意で複数添付可。原本保管・配布資料用
  - 保存先は**VPS上のサーバーディレクトリ**(DBにはファイルパスとメタ情報のみ保持)
  - バックアップ対象に含める(`age`暗号化バックアップの対象を DB＋添付ディレクトリに拡張。
    **DBダンプと同じcronにまとめる**。別建てにすると片方だけ失敗した際に気づきにくく、
    復元時のタイミングもずれるため)
  - **フェーズ1では添付ファイルの中身は検索対象外**(PDF内テキスト検索は実装コストが見合わないため)

#### 許可形式と容量(確定)
- **許可する拡張子**:PDF / Word(.docx .doc)/ Excel(.xlsx .xls)/ PowerPoint(.pptx .ppt)/
  画像(.jpg .jpeg .png .heic)
- **許可しない**:実行ファイル・スクリプト(.exe .bat .js .html 等)、圧縮ファイル(.zip 等)、動画
  - 実行ファイル・スクリプトは、マルウェアや偽ページの配布経路になり得るため
    (お知らせのURLに対するSafe Browsingチェック〈→ §7〉が添付経由で素通りされる)
  - 圧縮ファイルは展開するまで中身を検査できないため
  - 動画は容量とバックアップ負荷が大きいため。必要な場合はLINEグループへの直接投稿で代替
  - なお議事録を投稿できるのは管理者のみのため、不特定多数のアップロードに比べリスクは低い。
    その前提で画像・PowerPointまで許可範囲を広げている
- **容量上限**:1ファイル20MB / 1議事録あたり最大10ファイル

#### 画像の自動圧縮(確定)
容量を圧迫する主因は写真であるため、**画像のみサーバー側で自動圧縮**する。

- アップロード時に長辺を一定サイズへリサイズし、再エンコードして保存
- **HEICはJPEGへ変換**(閲覧互換性の確保も兼ねる)
- **PDF・Officeファイルは圧縮せず原本のまま保存**する
  - PDFの再圧縮はスキャン文書の可読性を損なうリスクがあるため
  - Officeファイルは内部が既にzip圧縮されており、再圧縮の効果がほぼないため
- 圧縮は不可逆であり、圧縮後のファイルが原本となる。原本を別途保持する二重管理は行わない
  (容量削減という目的と矛盾するため)

### 一覧画面(確定)
- 日付降順。カテゴリバッジ・日付・タイトル・本文冒頭・添付有無を表示
- カテゴリタブ(すべて / 全体協議会 / 役員協議会)で絞り込み
- 一般ユーザーには役員協議会タブが表示されず、一覧にも役員協議会の議事録は出ない

### 詳細画面(確定)
- カテゴリバッジ・日付・タイトル・投稿者・最終更新日時・本文・添付ファイル一覧
- 添付ファイルはアイコン付きリストで表示し、タップでダウンロード
- **管理者にのみ「編集」「削除」ボタンを表示**(閲覧用・管理用に画面を二重に作らない。§3の方針を踏襲)

### 検索(確定)
- キーワード検索:**タイトル＋本文**を対象
- 絞り込み:カテゴリ、日付範囲
- **検索処理そのものに権限フィルタをかける**。一般ユーザーの検索結果には役員協議会の議事録が一切ヒットしない(画面側で隠すのではなく、クエリ段階で除外する)

### 監査ログ
- 議事録の投稿・編集・削除を監査ログに記録
- ログには議事録IDを持たせ、**議事録単位での絞り込み**を可能にする(市場マスタの変更ログと同じ方式。→ §1 データ整合性の原則)

### お知らせへの自動連携(確定)
議事録の投稿を部会員に知らせるため、**議事録の投稿時にお知らせを自動生成**する。
議事録側に既読管理を作り込まず、既存のお知らせの既読管理(`announcement_reads`)に一本化するため。

- **生成タイミングは初回投稿時のみ**。編集のたびに通知が飛ぶのを防ぐ
- お知らせ本文は定型文＋議事録タイトル＋議事録詳細ページへの直リンク
- お知らせ一覧には**「議事録」カテゴリバッジ**を表示(既存の 全体/個人選果/共同選果 に並ぶ)
- 議事録の投稿にあたって、管理者が別途お知らせ投稿画面を開く必要はない

#### カテゴリ別の配信先(確定)
役員協議会の議事録は一般ユーザーに見せないため、由来するお知らせも同じ制御をかける。
**LINE配信先もカテゴリで振り分ける**点に注意(部会員グループへ流すと議事録の権限制御が無意味になる)。

| 議事録カテゴリ | サイト表示 | LINE配信先 |
|---|---|---|
| 全体協議会 | 全員 | 部会員のLINEグループ(10分保留後) |
| 役員協議会 | 管理者のみ | 管理者用LINEグループチャット(10分保留後) |

- 管理者用LINEグループは、§6「投稿があった時点で管理者用グループへ自動通知」で
  既に通知先として設計済み。追加のインフラは不要

#### 議事録由来のお知らせの取り扱い(確定)
議事録由来のお知らせは**議事録に紐づく従属データ**であり、独立した投稿ではない。
そのため手動投稿のお知らせとは操作系を分ける。

| 操作 | 結果 |
|---|---|
| 議事録を投稿 | お知らせを自動生成し、10分の保留を開始 |
| 保留中に議事録を編集 | お知らせの文面(タイトル・リンク)も追従し、**保留時間をリセット** |
| 議事録を削除 | 紐づくお知らせも連動して取り下げ |
| お知らせ側の個別削除 | **ボタンを出さない**(消す場合は議事録側から操作する) |

- 保留中の編集で保留時間がリセットされるのは、手動投稿の「訂正すると保留時間リセット」と同じ考え方
  (→ §6 投稿画面のUI)。タイトルの誤字に気づいた際、議事録ごと作り直さずに済むようにするため
- 議事録を削除しても、お知らせ・議事録とも**論理削除**とし、監査ログは残す(→ §1 データ整合性の原則)

---

## 9. コストの見立て

| 項目 | 方式 | ランニングコスト |
|---|---|---|
| ログイン認証 | LINEログイン | 無料 |
| お知らせ配信 | LINE公式アカウント(無料プラン) | 無料(月200通まで/想定利用量内) |
| データベース・サイト運用 | 既存VPS＋SQLite | 追加コストなし |
| バックアップ保管 | GitHub privateリポジトリ | 無料 |
| (検討中)写真取込OCR | Claude API利用の場合 | 月数十〜数百円程度(1日1〜2枚想定) |

- 参考:SMS認証を採用した場合、40名が月数回ログインする想定で月1,000〜2,000円程度のランニングコストが発生する見込みだった(不採用の判断材料)

---

## 10. 検討中の案件

### 販売通知の写真取込(OCR)
- 紙・FAXの販売通知をスマホで撮影 → OCRで構造化 → マトリクス入力フォームに読み取り結果をプリセット → 管理者が確認・修正して確定、という流れを検討中
- 手入力フォームが「OCR結果の確認・修正画面」を兼ねる構成のため、OCRを後付けしても入力画面は無駄にならない。読めない日は手入力にフォールバック
- **現在のアクション:実物の販売通知サンプルを収集中。集まり次第、読み取り精度を検証して採否を判断**
- エンジン候補:
  - **Claude API(画像入力)**:表構造の理解が得意で実装最短。月数十〜数百円。MVPの第一候補
  - **テンプレート方式のOSS(OpenCV位置合わせ＋Tesseract数字限定読み取り)**:フォーマット固定を活かし、セル座標を事前定義して数字だけ読む方式。無料だが座標定義・位置合わせの作り込みが必要。定着後のコストゼロ化(フェーズ2)候補
  - その他OSS:PaddleOCR(PP-Structure)、YomiToku(日本語帳票特化)など。FAX画質での精度は要検証
- 二段構え方針:フェーズ1はClaude APIで実装して運用定着を検証 → 定着後にOSS置き換えでランニングコストゼロ化を検討(確認画面・DBは使い回し、差し替えはエンジン部分のみ)
- 精度向上の工夫:撮影ガイド枠、FAX原紙が薄い場合は濃いコピーを取ってから撮影、合計値との検算による自動チェック

---

## 11. 検討したが不採用/保留になった案

- **SMS(Twilio等)によるOTP認証**:ランニングコストと国内SMSサービス利用時の法人登録の手間から不採用。LINEログインを採用
- **お知らせリンクのドメイン固定**:外部の有益情報共有がしにくくなるため却下。機械的な安全性チェック(Safe Browsing API)で代替
- **DB直接操作(phpMyAdmin等)によるデータ修正**:修正履歴が残らず監査ログを素通りするため不採用。修正は必ず管理画面経由とする
- **Cloudflare Access(メール認証でのアクセス制限)**:初期検討段階で出た案。LINEログイン採用後は設計の中心から外れているが、VPSへの直接アクセス遮断層として実装時に再検討の余地あり
- **ディスク単位の暗号化(LUKS等)**:優先度はバックアップ暗号化より低いという位置づけで、現時点では未確定
- **未読者へのLINE個別リマインド送信**:フェーズ1では保留。LINE公式アカウント無料プランは月200通までのため、個別リマインドを頻発させるとお知らせ配信の枠を圧迫する。まずは未読者の一覧表示のみを提供し、フォローアップの手段は管理者の判断に委ねる。実需が出てから実装判断する(OCRと同じ二段構えの考え方)
- **議事録の添付ファイル内テキスト検索**:フェーズ1では対象外。本文テキストでの検索が機能していれば実用上足りるという判断。必要になればPDFのテキスト抽出を後付けする
- **紐付け時に本人へ氏名を入力させる方式**:表記ゆれ(「山田太郎」「山田 太郎」)が発生すること、
  管理者の承認作業が導入時に40件発生すること、承認待ちの滞留が問い合わせを招くことから不採用。
  名簿からの選択方式を採用(→ §3 名簿とLINEアカウントの紐付け)
- **添付ファイルの全形式許可**:実行ファイル・スクリプトの配布経路になり得ること、
  圧縮ファイルは中身を検査できないこと、動画は容量・バックアップ負荷が大きいことから不採用。
  許可リスト方式とする(→ §8 許可形式と容量)
- **添付PDFの自動圧縮**:スキャン文書の可読性を損なうリスクがあるため見送り。画像のみ圧縮する

---

## 12. 今後決めること(未確定事項)

- 販売通知サンプルの収集と写真取込OCRの精度検証・採否判断(→ §10)
- VPS/GitHub/LINE Developersそれぞれの具体的なセットアップ手順
- 監査ログの日次レポートの配信方法・フォーマット詳細
- 引継ぎドキュメントの実際の作成・保管場所
- データ一覧ページのCSVダウンロードをスマホでどう案内するか(操作説明)
- ダッシュボード(閲覧画面)の**出荷量の表示単位**(現行モックはkg表示。データ一覧に合わせて個数+kg換算併記にするか要検討。単価は個口単価に統一済み)
- 議事録由来のお知らせの定型文面(タイトルの付け方、本文テンプレート)
- 議事録を論理削除した際、紐づくお知らせが既にLINE配信済みだった場合の扱い
  (サイトからは取り下がるが、配信済みLINEメッセージは取り消せない点の運用上の整理)
- 画像圧縮時のリサイズ後サイズ(長辺の具体的なピクセル数)と圧縮品質の設定値
- 名簿の初期登録方法(部会名簿からのCSV一括取込を用意するか、画面から1名ずつ入力するか)
- LINE連携が完了しない部会員が残った場合の運用(紙での代替案内を出すかどうか)

---

## 13. 実装タスクの大枠(モックからの逆算)

1. **基盤準備**:LINE Developersアプリ登録、LINE公式アカウント作成と管理者グループへの招待(groupId取得)、VPS上にSQLite環境構築、本番用GitHubリポジトリ作成
2. **DB設計**:マスタ(部会員・市場・サイズ・荷姿・選果方法・**議事録カテゴリ**)、日次実績テーブル、修正履歴、お知らせ+`announcement_reads`、**議事録+添付ファイル**、監査ログ。荷姿マスタにkg換算係数(200gパック=0.2、3kgバラ=3.0)を持たせる。単価は個口単価で保存し、円/kgは表示時に換算(200g=×5、3kg=÷3)
3. **認証**:LINEログイン実装＋初回の名簿紐付けフロー(名簿からの選択方式)。
   氏名表示は名簿の値を正とし、LINE表示名は使用しない
4. **閲覧画面**:モックUIを実データ接続に置き換え(タブ・日付ピル・グラフ・前年比・加重平均)
5. **データ一覧ページ**:期間プルダウン・チェックボックスフィルタ・CSV出力(モック`list.html`が仕様の正)
6. **管理画面**:マトリクス入力フォーム(入力チェック・下書き・前日コピー・修正履歴。モック`admin.html`が仕様の正)、お知らせ投稿(10分保留・訂正/削除・Safe Browsingチェック・**既読/未読の内訳表示**。モック`announce.html`が仕様の正)、市場の管理(追加・論理削除・並べ替え。モック`markets.html`が仕様の正)、**議事録(2カテゴリ・権限別表示・本文検索・添付ファイル。モック`minutes.html`/`minutes-detail.html`/`minutes-edit.html`が仕様の正)**、**部会員名簿の管理(氏名・権限・LINE連携状況・論理削除。モック`members.html`が仕様の正)**
7. **サイト内ナビゲーション**:管理者用/一般ユーザー用の共通ナビ帯、未保存離脱ガードの共通コンポーネント化
8. **運用系**:監査ログ日次レポート、`age`暗号化バックアップのcron化(**添付ファイルディレクトリを対象に含める**)、引継ぎドキュメント
9. **(採否判断後)写真取込OCR**

---

## 14. 進め方の運用ルール(このプロジェクト全体に適用)

- Claude(このチャット):仕様検討・設計・資料作成
- Claude Code:実装・VPS操作・GitHub操作などの実行作業
- Yoppyさん:GO/NO-GO判断
- 資料は「企画書」ではなく「提案メモ」という位置づけ。承認を取るための硬い文書ではなく、部会内でフランクに意見をもらうためのたたき台
